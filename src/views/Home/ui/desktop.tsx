"use client";

// import { useTranslations } from "next-intl";

// import { useI18nRouter } from "@/shared/lib/routes";
// import Button from "@/shared/ui/Button/Button";
// import { Typography } from "@/shared/ui/Typography";

export const DesktopHomePage = () => {
  // const t = useTranslations("home");
  // const { push, changeCountry, changeLocale, country } = useI18nRouter();

  return (
    <div>
      {/* <Button onClick={() => changeCountry("kg")}>KG Country</Button>
      <Button onClick={() => changeCountry("ru")}>RU Country</Button>

      <Button onClick={() => changeLocale("ky")}>KY Lang</Button>
      <Button onClick={() => changeLocale("ru")}>RU Lang</Button>

      <Button onClick={() => push("/")}>Go Home</Button> */}
   

      {/* <Typography>{country}</Typography>
      <Typography>{t("removed")}</Typography> */}
    </div>
  );
};
