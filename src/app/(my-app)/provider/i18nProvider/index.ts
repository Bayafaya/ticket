import {
  routing,
  Link,
  redirect,
  usePathname,
  useRouter,
  getPathname,
} from "./lib/routing";
import { DEFAULT_LOCALE, LOCALES, NAMESPACES } from "./model/types/i18nTypes";

export {
  routing,
  Link,
  redirect,
  usePathname,
  useRouter,
  getPathname,
  DEFAULT_LOCALE,
  LOCALES,
  NAMESPACES,
};
