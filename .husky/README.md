# Husky hooks

- `pre-commit`: starting eslint и type check
- `pre-push`: starting tests

# После любого создания или изменения хука - запустите команду для установки прав на выполнение:
# chmod +x .husky/<название>